import styled from 'styled-components'

export const Logo = styled.img`
  padding-left: 5rem;
  width: 10rem;
  cursor: pointer;
  transition: all 2s;
  &:hover {
    transform: translateX(10rem);
  }
`
