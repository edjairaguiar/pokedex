import styled from 'styled-components'

export const Wrapper = styled.div`
  width: 60rem;
  height: 6rem;
  display: block;
  flex-direction: row;
  align-items: center;
`

export const LEDPainel = styled.ul`
  list-style-type: none;
  overflow: hidden;
`

export const Light = styled.li`
  display: inline;
`
